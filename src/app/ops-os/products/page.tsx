import { DepartmentCommand } from "../DepartmentCommand";

export const metadata = { title: "Products Division | KHG Ops OS" };

export default function ProductsPage() {
  return <DepartmentCommand divisionKey="products" />;
}
