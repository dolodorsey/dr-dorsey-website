import { DepartmentCommand } from "../DepartmentCommand";

export const metadata = { title: "Casper Group | KHG Ops OS" };

export default function CasperPage() {
  return <DepartmentCommand divisionKey="casper" />;
}
