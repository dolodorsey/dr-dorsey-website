import Workspace from "../Workspace";

export const metadata = { title: "Content Studio | KHG Ops OS" };

export default function ContentStudioPage() {
  return <Workspace section="content-studio" />;
}
