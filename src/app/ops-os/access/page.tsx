import AccessControl from "./AccessControl";

export const dynamic = "force-dynamic";

export const metadata = { title: "KHG Ops OS | App Access Control" };

export default function AccessPage() {
  return <AccessControl />;
}
