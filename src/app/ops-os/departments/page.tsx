import { DepartmentCommand } from "../DepartmentCommand";

export const metadata = { title: "Department OS | KHG Ops OS" };

export default function DepartmentsPage() {
  return <DepartmentCommand divisionKey="departments" />;
}
